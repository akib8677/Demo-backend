import { addProduct, getProducts, deleteProduct,updateProduct } from './products.controller';
import multer from 'multer';

module.exports = {
    addProduct,
    getProducts,
    deleteProduct,
    updateProduct,
    multer
}